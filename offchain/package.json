{
  "name": "dwell-offchain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test",
    "typecheck": "tsc --noEmit",
    "fixture": "node scripts/generate-merkle-fixture.ts"
  },
  "dependencies": {
    "@openzeppelin/merkle-tree": "^1.0.8"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
